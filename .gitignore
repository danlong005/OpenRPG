rpgc
rpgc_debug
.DS_Store
build/*.o
build/*.o.tmp
build/lexer.cpp
build/parser.cpp
build/parser.h
*.dSYM/
.vscode/
/*.cpp
/*.o
/test[0-9]*
# Program-described (I-spec/O-spec) flat-file test runtime output — see
# TODO.md item #4; these land in the repo root because the flat-file
# runtime opens paths relative to CWD, and run_tests.sh doesn't cd.
/testfl*.txt

# IBM i conformance run transcript — 1.7MB of raw compiler listings,
# regenerated by scripts/ibmi-conformance.sh on every run. The distilled
# result lives in ibmi-conformance-report.txt and TODO.md.
ibmi-conformance-transcript.txt
