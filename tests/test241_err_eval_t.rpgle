**FREE
// (T) is an internal marker the fixed-format transpiler puts on the EVAL
// an ADD or Z-ADD becomes; written by hand it is not an RPG extender.
DCL-S n PACKED(3:0);
EVAL(T) n = 1000;
*INLR = *ON;
